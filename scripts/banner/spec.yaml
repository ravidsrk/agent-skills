# agent-skills banner — Cluster A (ASCII Terminal)
# Render: bash skills/terminal-poster/scripts/make-poster.sh .banner-spec.yaml assets/banner.png

cluster: a

title: agent-skills
bottom_tagline: "five skills. one repo. zero glue code."
handle: "@ravidsrk"

panels:
  - label: INFRASTRUCTURE
    subject: dns + cloud migration
    tagline: from registrar to ECS in 7 phases
    flow: "fly.io ──▶ cutover ──▶ aws"
    prose: "battle-tested playbooks for the boring-but-critical work. ship to AWS without losing users, move DNS to Cloudflare without losing weekends."
    items:
      - "cloudflare-dns — migrate + harden zones"
      - "namecheap-dns — XML API with wholesale-replace guards"
      - "fly-to-aws-migration — 7 phases, ≤9 min downtime"
      - "battle-tested in production (2026)"

  - label: RESEARCH
    subject: 8-source parallel orchestrator
    tagline: cross-source evidence in 90 seconds
    flow: "topic ──▶ 8 sources ──▶ quotable JSON + markdown"
    prose: "fans out across X, Reddit, HN, GitHub, Polymarket, YouTube w/ transcripts, and Exa neural search. one auth, one balance, one command."
    items:
      - "deep-research — parallel multi-source"
      - "powered by monid (https://monid.dev)"
      - "structured evidence + human-readable dump"
      - "~$0.10 per run · ~60-90s wall time"

  - label: CREATIVE
    subject: viral image generator
    tagline: terminal aesthetic posters in 30s
    flow: "yaml spec ──▶ nano banana pro ──▶ image"
    prose: "5 reusable templates A-E in retro cyberpunk style. pixel-bitmap headlines, ASCII box-drawing, brutalist minimalism. the look that goes viral on X."
    items:
      - "terminal-poster — 5 cluster templates"
      - "cluster A audited at 99% fidelity"
      - "nano banana pro via OpenRouter"
      - "~$0.002 + ~30s per poster"
