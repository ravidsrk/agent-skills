Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/backlog-zero" exactly once; bottom-center a thin line and the caption "every issue closed with evidence" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #2563EB. Scene: an isometric tall stack of small issue cards at left steadily feeding one-by-one through a processing arch (verify, fix, review, merge) and emerging as flat closed-and-checkmarked cards into an empty tray at right labeled with a large 0; a small loop arrow returns from the tray to the stack (re-enumerate); abstract dot patterns instead of text
