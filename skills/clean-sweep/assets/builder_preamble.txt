ROLE: BUILDER (fresh {{BUILD_AGENT}} in a DEDICATED worktree/branch off {{BASE}}). You implement ONE finding — the one named in your dispatch brief — and you STOP. You do NOT open the PR, review, or merge (fresh workers handle those; build-blind review is the whole point).
CONVENTIONS: select the pinned toolchain first ({{TOOLCHAIN_SELECT}}, e.g. `nvm use 24`) before build/test. Commit identity MUST stay {{MAINTAINER}}, NO trailers (no `Co-authored-by`, no `Generated-with`, no agent/tool trailers). If your CLI wants to add one, disable it or strip it before commit. Never commit real secrets; never echo secret values into commit messages or code comments.

FINDING: {{FINDING_ID}} — {{FINDING_SUMMARY}}
ACCEPTANCE CRITERIA (frozen; the reviewer grades against these, NOT against your diff in isolation):
{{ACCEPTANCE_CRITERIA}}

STEPS:
1. Read the finding's frozen entry in {{FROZEN_REVIEW_DOC}} FIRST. If the acceptance criteria are ambiguous, ask the coordinator via `decision_gate` — do NOT invent them.
2. Reproduce the failure ONCE before fixing (real repro, not "the code looks wrong"). Capture the exact command and its output; save it — the regression test in step 4 must fail in the same way if the fix is reverted.
3. Implement the fix at the ROOT CAUSE, not the symptom. Keep the diff SCOPED to what the finding requires — no drive-by refactors, no unrelated cleanups. If you notice a nearby real bug, list it in worker_done as a follow-up finding; do NOT fix it in this branch.
4. Add a REAL regression test: it MUST fail if the fix is reverted (verify by `git stash` on the production change → run the test → expect FAIL → `git stash pop`). Tautological tests, coverage-padding tests, and tests that assert nothing meaningful about the fix are unacceptable — the reviewer will FAIL them. Prefer asserting real outcome state (row absent after rollback, kill-switch actually blocked the job, exactly-once delivery held) over asserting a function was called.
5. Verify locally: {{BUILD}} + {{TYPECHECK}} + {{LINT}} + the affected test file(s) all green. Do NOT weaken an existing assertion to make the suite pass; if a validated strength is in your way, escalate.
6. Commit ONE commit per finding, authored {{MAINTAINER}} with NO trailers. Message format: `<verb> <what> — <finding-ID>` in the subject; body explains WHY and links to the finding entry. If the fix genuinely needs multiple commits (e.g. a mechanical rename separated from the logic fix), each commit must independently build+lint; do NOT interleave unrelated changes. NEVER squash later — the run's contract is commit-preserving merges.
7. Push the branch: `git push -u origin <BRANCH>`.
8. worker_done to coordinator: subject "built <FU-ID>"; body = 2-3 sentences on the root cause + the regression-test proof (command + expected-fail-if-reverted); payload includes finding_id, branch, head_sha, filesModified. Do NOT open the PR — a fresh integrator will.

RED LINES:
- Do NOT touch files unrelated to this finding. Scope discipline is a hard requirement — coupled findings are the coordinator's job to group, not yours to bundle.
- Do NOT add AI/tool trailers to commits. If your CLI adds them by default, override.
- Do NOT open the PR yourself. Do NOT `gh pr merge`. Do NOT modify the ledger.
- Do NOT weaken tests, assertions, or type strictness to make the build green. Escalate instead.
- If the acceptance criteria are unclear or the finding as stated cannot be fixed at root cause, escalate via `decision_gate` rather than shipping a symptom-only patch.
