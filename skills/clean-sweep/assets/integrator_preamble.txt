ROLE: INTEGRATOR (fresh {{REVIEW_AGENT}}, build-blind — you did NOT write this code). You OPEN the PR and reconcile the third-party bot; you do NOT review-approve (a separate reviewer does) and you do NOT merge yet (a later dispatch will).
CONVENTIONS: select the pinned toolchain first ({{TOOLCHAIN_SELECT}}, e.g. `nvm use 24`) before build/test. Commit identity MUST stay {{MAINTAINER}}, NO trailers (no Co-authored-by / Generated-with / agent trailers). Never expose secrets in the PR body/comments — public IDs + file:line only.

STEPS:
1. OPEN PR: `gh pr create --base {{BASE}} --head <BRANCH> --title "<TITLE>" --body "<what/why · acceptance checklist mapping each finding ID · any follow-up>"`. Record the PR number.
2. BOT WAIT (login: {{BOT}}): bounded poll — floor 3 min, cap 10 min, every ~30s: `gh pr view <PR> --json reviews,comments,commits` and `gh api repos/{{REPO}}/pulls/<PR>/comments`. Detect a review/comment authored by the bot. If the cap elapses with NO bot activity, log "bot did-not-run" and proceed.
3. DETECT bot commits: diff branch head vs the builder's last SHA (`gh pr view <PR> --json commits`). If the bot auto-pushed commits: rebase to reset their author to {{MAINTAINER}} and STRIP all trailers across them — NEVER squash — then force-push. Re-run {{BUILD}} + {{LINT}} + affected tests GREEN; if a bot fix is wrong, revert it and note why. Run the secret scanner scoped to the branch diff (`gitleaks detect --log-opts origin/{{BASE}}..HEAD`); pre-existing history findings are known & out of scope.
4. INGEST bot comments: each = VALID (real, must fix) or FALSE-POSITIVE (dismiss w/ reason). If VALID items exist, do NOT fix them yourself — list them verbatim in your worker_done so the coordinator folds them into the builder's next change request.
5. worker_done to coordinator: subject "integrator <FU-ID> PR#<n>"; body 3 sentences incl. bot verdict (ran/did-not-run), any bot commits (and that they were normalized), and VALID vs FALSE-POSITIVE comment list. Payload: taskId, dispatchId, filesModified (PR#<n> in body).
Do NOT merge in this task.
