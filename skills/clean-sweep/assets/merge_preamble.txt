ROLE: INTEGRATOR MERGE (fresh {{REVIEW_AGENT}} in the finding's worktree). The PR is REVIEWED=approved (coordinator-confirmed) and bot-reconciled. Perform a conflict-aware, commit-preserving merge into BASE, then verify.
CONVENTIONS: select the pinned toolchain ({{TOOLCHAIN_SELECT}}, e.g. `nvm use 24`) before build/test. All commits must be authored {{MAINTAINER}}, NO trailers.
STEPS:
1. Check conflicts: `gh pr view <PR> --json mergeable,mergeStateStatus`. IF conflicts / behind BASE: `git fetch origin {{BASE}} && git rebase origin/{{BASE}}`, resolve preserving BOTH the change intent AND what landed on BASE; keep every commit authored {{MAINTAINER}} w/ no trailers; re-run {{BUILD}} + {{LINT}} + affected tests green; force-push. (If resolution materially changed logic, say so in worker_done so the coordinator can re-review.)
2. FREEZE-CHECK for LATE bot commits (the bot keeps pushing even after review): `git fetch` + `git log origin/{{BASE}}..HEAD --format='%h %an <%ae>'`. Normalize any {{BOT}}-authored commit to {{MAINTAINER}} (reset author+committer, strip trailers, NO squash). CRUCIAL: a late rider is NOT always trivial — READ each one. If it's mechanical (author fix, missing mock, lint), normalize + keep. If it's an UNREVIEWED LOGIC change (the design reviewer never saw it), DISCARD it: reset the branch to the reviewed+green head and re-land that logic later as a separate reviewed PR. NEVER merge unreviewed logic just because the bot wrote it. MECHANICAL-FIX PRE-AUTHORIZATION: if the final head is red only on a mechanical break (missing test-mock, lint), apply the small behavior-preserving fix yourself as one {{MAINTAINER}} commit + re-verify — do NOT raise a decision_gate for mechanical fixes.
3. MERGE (preserve commits, NEVER squash): `gh pr merge <PR> --merge --delete-branch`. If the bot's Autofix is still ON and racing you (a rider lands between your force-push and merge): force-push the reviewed+green head and IMMEDIATELY `gh pr merge` (merging deletes the branch → ends the loop); retry up to 3× if a rider wins the race; then confirm the merge commit's second parent == your reviewed head (NOT a rider). `--delete-branch` may fail LOCALLY when the head is checked out in this worktree while the server merge SUCCEEDS — then delete the remote ref via `gh api -X DELETE repos/{{REPO}}/git/refs/heads/<branch>`.
4. VERIFY: `git fetch origin {{BASE}}`; confirm the merge commit is present, ALL branch commits preserved, every commit authored {{MAINTAINER}} with NO trailers, and the head branch is deleted (`gh pr view <PR> --json state,mergedAt,headRefName`).
5. worker_done to coordinator: subject "merged <FU-ID> PR#<n>"; body = confirmation of merge commit + author/hygiene + branch deletion. Payload: taskId, dispatchId.
Do NOT remove the worktree — the coordinator does WT_CLEAN.

IMPORTANT (self-approval constraint): everything runs under one shared GitHub account, so `gh pr review --approve` is blocked ("can't approve your own PR"). Do NOT wait for GitHub's reviewDecision=APPROVED — the coordinator has already confirmed the internal reviewer's PASS. `gh pr merge <PR> --merge --delete-branch` still works regardless of review state. If merge is blocked by a REQUIRED-review branch-protection rule (a real rule, not just missing approval), report it via escalation rather than forcing.

KNOWN NON-BLOCKING CHECKS (do NOT gate on them, do NOT ask): the coordinator will supply a list of pre-existing baseline CI failures (e.g. an inherited lint/dupe-key error being fixed by a separate baseline-green fix-unit). Per this run's baseline gate, green == no NEW failures vs baseline. If your PR's ONLY red check is a known-baseline failure and your diff doesn't touch that file, proceed to merge — `gh pr merge --merge` succeeds even when mergeStateStatus is UNSTABLE.
