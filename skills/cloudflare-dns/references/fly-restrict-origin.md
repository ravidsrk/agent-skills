# Restrict Fly origin to Cloudflare-only traffic

Full walkthrough (middleware, Caddy/nginx, Tunnel): **`scripts/fly-restrict-origin.md`**.

Read it **before** locking the origin — misconfiguration can lock you out of your own apps.
