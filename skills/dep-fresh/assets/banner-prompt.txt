Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/dep-fresh" exactly once; bottom-center a thin line and the caption "every major current, CI green the whole way" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #CA8A04. Scene: an isometric row of package boxes on a conveyor, each being lifted and swapped for a taller newer box one at a time, with a green check gate the conveyor must pass through after each swap; one box is set aside on a small parked shelf with a pin icon; abstract dot patterns instead of text
