Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/docs-truth" exactly once; bottom-center a thin line and the caption "every claim traces to the tree" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #0284C7. Scene: an isometric open manual on the left with lines that each shoot a thin verification beam rightward to matching source-code cards; most beams land on a card with a checkmark, one beam hits nothing and its doc line is being struck through; abstract dot patterns instead of text
