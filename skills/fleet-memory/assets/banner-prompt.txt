Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/fleet-memory" exactly once; bottom-center a thin line and the caption "run N+1 remembers what run N paid for" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #D97706. Scene: an isometric open ledger book at center with orderly dotted lines; from its pages, small luminous note cards flow rightward into the top of three fresh terminal cards; behind the book a faded stack of older pages recedes (the pruned past); one note card carries a small checkmark echo returning from a terminal; abstract dot patterns instead of text
