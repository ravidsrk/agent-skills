Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/gate-steward" exactly once; bottom-center a thin line and the caption "the right resolver for every gate" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #059669. Scene: three isometric gates side by side: a small open gate with a checkmark token passing through, a medium gate with a neat stack of three tokens waiting on a tray, and a tall closed gate with a human figure silhouette holding the only key; a slim ledger book in the foreground with dot-pattern lines; abstract dot patterns instead of text
