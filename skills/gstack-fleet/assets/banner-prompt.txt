Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/gstack-fleet" exactly once; bottom-center a thin line and the caption "one fleet, any gstack command" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #10B981. Scene: a single isometric dispatcher hub at center with a swappable command dial on its face; from it a fan of identical worker terminal cards spreads right, each running the same command, converging into one stacked report card at far right; a small slot on the hub shows interchangeable command chips queued to load; abstract dot patterns instead of text.
