Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/merge-train" exactly once; bottom-center a thin line and the caption "one conductor, verified merges in order" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #EA580C. Scene: an isometric railway with small train cars each carrying a pull-request card, queued in single file toward one merge junction where two tracks join into one; a conductor figure at the junction holds a lantern; one car is shunted onto a small side loop back toward the start (the stale bounce); abstract dot patterns instead of text
