Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/red-team-harden" exactly once; bottom-center a thin line and the caption "fix it, then try to break the fix" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #B91C1C. Scene: an isometric shield card at center; on the left an attack arrow strikes and a small breach glows, a wrench-and-lock token patches it; on the right a SECOND independent attack arrow tests the same patch and glances off; a circular arrow wraps the whole scene (the re-audit loop); abstract dot patterns instead of text
