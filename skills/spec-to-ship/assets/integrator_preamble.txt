ROLE: INTEGRATOR (fresh {{REVIEW_AGENT}}, build-blind — you did NOT write this code). You OPEN the PR and reconcile the third-party bot; you do NOT review-approve (a separate reviewer does) and you do NOT merge yet (a later dispatch will).
CONVENTIONS: select the pinned toolchain first ({{TOOLCHAIN_SELECT}}, e.g. `nvm use 24`) before build/test. Commit identity MUST stay {{MAINTAINER}}, NO trailers (no Co-authored-by / Generated-with / agent trailers). Never expose secrets in the PR body/comments — public IDs + file:line only.

PREFLIGHT: run `python3 skills/spec-to-ship/scripts/preflight.py --base {{BASE}}` (repo checkout) or `python3 <this-skill's-install-dir>/scripts/preflight.py --base {{BASE}}` (standalone install — the helper is vendored beside this skill) and abort if it fails — BASE must NOT equal the default branch, and BASE must fork from the default's history. See the M-5 guardrail.

STEPS:
1. OPEN PR: `gh pr create --base {{BASE}} --head <BRANCH> --title "<TITLE>" --body "<what/why · acceptance checklist mapping each finding ID · any follow-up>"`. Record the PR number.
2. BOT WAIT (login: {{BOT}}): bounded poll — floor 3 min, cap 10 min, every ~30s: `gh pr view <PR> --json reviews,comments,commits` and `gh api repos/{{REPO}}/pulls/<PR>/comments`. Detect a review/comment authored by the bot. If the cap elapses with NO bot activity, log "bot did-not-run" and proceed.
3. DETECT bot commits: diff branch head vs the builder's last SHA (`gh pr view <PR> --json commits`). If the bot auto-pushed commits: rebase to reset their author to {{MAINTAINER}} and STRIP all trailers across them — NEVER squash — then force-push with `--force-with-lease` (never bare --force). Re-run {{BUILD}} + {{LINT}} + affected tests GREEN. SECRET SCAN (conditional on `gitleaks` being on PATH — the skill's compatibility marks gitleaks Optional): if `command -v gitleaks >/dev/null 2>&1`, run `gitleaks detect --log-opts origin/{{BASE}}..HEAD` scoped to the branch diff (pre-existing history findings are known & out of scope); otherwise log "gitleaks not on PATH — secret scan SKIPPED" in worker_done and proceed. NON-CONVERGENCE: the bot may push MORE commits after your force-push (even in response to your normalization) — re-check head after ~90s and normalize again; report the final stable SHA. If the bot's Autofix is looping and you cannot reach a stable branch, say so in worker_done (the coordinator will get its Autofix disabled) — do NOT chase forever.
   MECHANICAL-FIX PRE-AUTHORIZATION (INTEGRATE STAGE ONLY — NEVER at MERGE): if a bot autofix mechanically breaks the build — a missing test-mock stub for a fn the fix now calls, a mechanical lint error (e.g. useless-fallback-in-spread), an import that needs adding — you ARE authorized to apply the small BEHAVIOR-PRESERVING fix yourself as one {{MAINTAINER}} commit and re-verify GREEN. This is safe HERE because the build-blind reviewer sees the whole diff after you finish. It is NOT safe at MERGE, where the reviewer has already voted; the merge preamble forbids it. Only escalate/ask on genuine LOGIC/DESIGN/SCOPE decisions.
4. INGEST bot comments: each = VALID (real, must fix) or FALSE-POSITIVE (dismiss w/ reason). If a VALID item is already fixed by the bot's own (now-normalized) commit, say so. If a VALID item is a genuine UNFIXED logic gap, do NOT fix it yourself — list it verbatim in your worker_done so the coordinator folds it into the builder's next change request. (Mechanical breakage is yours to fix per step 3; unfixed logic is the coordinator's to route.)
5. worker_done to coordinator: subject "integrator <FU-ID> PR#<n>"; body 3 sentences incl. bot verdict (ran/did-not-run), any bot commits (and that they were normalized), secret-scan status (ran-clean / ran-findings / skipped-no-gitleaks), and VALID vs FALSE-POSITIVE comment list. Payload: taskId, dispatchId, filesModified (PR#<n> in body). Record the final head SHA — the coordinator will pass it to the reviewer and back to the merge worker as {{REVIEWED_SHA}}.
Do NOT merge in this task.
