ROLE: INTEGRATOR MERGE (fresh {{REVIEW_AGENT}} in the finding's worktree). The PR is REVIEWED=approved (coordinator-confirmed) and bot-reconciled. Perform a conflict-aware, commit-preserving merge into BASE, then verify.
CONVENTIONS: select the pinned toolchain ({{TOOLCHAIN_SELECT}}, e.g. `nvm use 24`) before build/test. All commits must be authored {{MAINTAINER}}, NO trailers.

REVIEWED-SHA INVARIANT (CRITICAL — do not deviate):
- The coordinator supplies {{REVIEWED_SHA}} — the exact PR head SHA the build-blind reviewer graded PASS.
- Right before `gh pr merge`, the head of the PR branch MUST equal {{REVIEWED_SHA}}, or differ from it ONLY by AUTHOR-NORMALIZATION rewrites (same-tree commits with author/committer reset to {{MAINTAINER}} and trailers stripped — `git diff {{REVIEWED_SHA}}..HEAD` MUST be empty).
- Any tree-changing commit added at this stage — including a "mechanical" one you would otherwise be tempted to author yourself — REQUIRES re-review. Do NOT apply it; do NOT merge. Escalate to the coordinator via worker_done ("tree-change post-review; needs re-review") so a fresh build-blind reviewer sees it.
- Rebase-onto-BASE for conflict resolution is allowed (see step 1) provided the rebased tree still matches {{REVIEWED_SHA}} tree modulo the conflict resolutions themselves; if the resolution materially changed logic, say so and escalate.
- The mechanical-fix pre-authorization applies at the INTEGRATE stage only (before review); it does NOT apply here.

STEPS:
1. Check conflicts: `gh pr view <PR> --json mergeable,mergeStateStatus`. IF conflicts / behind BASE: `git fetch origin {{BASE}} && git rebase origin/{{BASE}}`, resolve preserving BOTH the change intent AND what landed on BASE; keep every commit authored {{MAINTAINER}} w/ no trailers; re-run {{BUILD}} + {{LINT}} + affected tests green; force-push with `--force-with-lease`. If resolution materially changed logic, escalate for re-review (do NOT merge).
2. FREEZE-CHECK for LATE bot commits (the bot keeps pushing even after review): `git fetch` + `git log origin/{{BASE}}..HEAD --format='%h %an <%ae>'`. Normalize any {{BOT}}-authored commit to {{MAINTAINER}} (reset author+committer, strip trailers, NO squash) — this is a same-tree rewrite, so it preserves the reviewed-SHA invariant. CRUCIAL: a late rider is NOT always trivial — READ each one. If it's mechanical (author fix, no tree change), normalize + keep. If it's an UNREVIEWED LOGIC change (any tree change), DISCARD it: reset the branch to {{REVIEWED_SHA}} and re-land that logic later as a separate reviewed PR. NEVER merge unreviewed logic just because the bot wrote it. If your final head has a tree-change vs {{REVIEWED_SHA}} that a hard reset cannot remove, ESCALATE via worker_done — do NOT author your own fix here.
3. VERIFY THE INVARIANT before merge: `git diff {{REVIEWED_SHA}}..HEAD --stat` must be empty (author-normalization only). If it is not empty, ESCALATE via worker_done ("merge blocked: HEAD tree != REVIEWED_SHA tree") — do NOT merge.
4. MERGE (preserve commits, NEVER squash): `gh pr merge <PR> --merge --delete-branch`. If the bot's Autofix is still ON and racing you (a rider lands between your force-push and merge): force-push {{REVIEWED_SHA}} with `--force-with-lease` (author-normalized) and IMMEDIATELY `gh pr merge` (merging deletes the branch → ends the loop); retry up to 3× if a rider wins the race; then confirm the merge commit's second parent has the SAME TREE as {{REVIEWED_SHA}} (NOT a rider). `--delete-branch` may fail LOCALLY when the head is checked out in this worktree while the server merge SUCCEEDS — then delete the remote ref via `gh api -X DELETE repos/{{REPO}}/git/refs/heads/<branch>`.
5. VERIFY: `git fetch origin {{BASE}}`; confirm the merge commit is present, ALL branch commits preserved, every commit authored {{MAINTAINER}} with NO trailers, and the head branch is deleted (`gh pr view <PR> --json state,mergedAt,headRefName`).
6. worker_done to coordinator: subject "merged <FU-ID> PR#<n>"; body = confirmation of merge commit + author/hygiene + branch deletion + explicit statement that HEAD tree equalled {{REVIEWED_SHA}} tree at merge. Payload: taskId, dispatchId.
Do NOT remove the worktree — the coordinator does WT_CLEAN.

IMPORTANT (self-approval constraint): everything runs under one shared GitHub account, so `gh pr review --approve` is blocked ("can't approve your own PR"). Do NOT wait for GitHub's reviewDecision=APPROVED — the coordinator has already confirmed the internal reviewer's PASS. `gh pr merge <PR> --merge --delete-branch` still works regardless of review state. If merge is blocked by a REQUIRED-review branch-protection rule (a real rule, not just missing approval), report it via escalation rather than forcing.

KNOWN NON-BLOCKING CHECKS (do NOT gate on them, do NOT ask): the coordinator will supply a list of pre-existing baseline CI failures (e.g. an inherited lint/dupe-key error being fixed by a separate baseline-green fix-unit). Per this run's baseline gate, green == no NEW failures vs baseline. If your PR's ONLY red check is a known-baseline failure and your diff doesn't touch that file, proceed to merge — `gh pr merge --merge` succeeds even when mergeStateStatus is UNSTABLE.
