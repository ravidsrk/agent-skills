Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/standing-fleet" exactly once; bottom-center a thin line and the caption "scheduled fleets that run themselves" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #7C3AED. Scene: a wall clock face at left emitting a left-to-right conveyor of small isometric terminal cards; a small gate arm before the conveyor (the precheck) lets some cards through and quietly drops one below; the rightmost card unfolds into a fan of three tiny worker terminals; abstract dot patterns on card faces instead of text
