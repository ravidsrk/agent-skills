Clean minimalist isometric illustration, Stripe documentation aesthetic, NOT photorealistic, NOT cyberpunk, no dense text. 2:1 aspect ratio (~1200x600). Soft warm cream background #F8F4EE with a faint horizontal gradient. Dark slate ink #1F2937. White #FFFFFF card faces with subtle drop shadows. Top-left small monospace label "skills/test-debt-zero" exactly once; bottom-center a thin line and the caption "a red-capable test on every critical path" exactly once. Do not duplicate any label; do not render any other words or fake domain names. Accent color: #0D9488. Scene: an isometric branching call-graph of nodes flowing left to right; a few core nodes on the main path glow and each gets a small shield-test token clipped onto it; one token is shown mid-revert with a red spark proving it fails when the code is pulled; abstract dot patterns instead of text
